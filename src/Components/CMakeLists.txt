# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(VanishingPointsEstimator)

ADD_COMPONENT(ROSProxy)

ADD_COMPONENT(MatTransform)

ADD_COMPONENT(TransformGenerator)

ADD_COMPONENT(Stitcher)

ADD_COMPONENT(Warper)

ADD_COMPONENT(ModelReader)

ADD_COMPONENT(POI)

ADD_COMPONENT(KeyholeDetector)

ADD_COMPONENT(PlaneGenerator)
